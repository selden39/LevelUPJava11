package generic;
public class FindMinDemo<T extends Comparable> {
  /*  public static void main(String[] args) {
        FindMinDemo<Integer> findInt = new FindMinDemo<>();
        findInt.findMin (2,6,9);
    }

    private void findMin(T... array){
        T min = array[0];
        for (T x : array) {
            if (x < min){

            }
        }

    }
*/
}
